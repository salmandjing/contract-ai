# Contract AI Platform V5 - Minimal Dependencies
# Only the essential packages needed to run the application

# AWS SDK and Bedrock (REQUIRED)
boto3>=1.34.0
botocore>=1.34.0

# Environment configuration (REQUIRED)
python-dotenv>=1.0.0

# PDF processing (REQUIRED for file uploads)
PyPDF2>=3.0.0

# HTTP requests (REQUIRED for API calls)
requests>=2.31.0

# OpenTelemetry for observability (REQUIRED for AgentCore)
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0

# Data validation (REQUIRED)
pydantic>=2.0.0

# YAML support (REQUIRED for config files)
pyyaml>=6.0.0